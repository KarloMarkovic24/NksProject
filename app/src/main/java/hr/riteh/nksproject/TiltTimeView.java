package hr.riteh.nksproject;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

import androidx.annotation.Nullable;

public class TiltTimeView extends View {
    public TiltTimeView(Context context) {
        super(context);
    }

    public TiltTimeView(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public TiltTimeView(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public TiltTimeView(Context context, @Nullable AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
